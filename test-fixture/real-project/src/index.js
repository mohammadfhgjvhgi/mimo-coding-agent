const calc = require('./calc');
const str = require('./string-utils');
const arr = require('./array-utils');
const logger = require('./logger');
const config = require('./config');
module.exports = { calc, str, arr, logger, config };
