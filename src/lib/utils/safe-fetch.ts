// Re-export — the canonical implementation lives at @/lib/safe-fetch.
// Kept here for backward compat with code that imports from @/lib/utils/safe-fetch.
export { safeFetch, ApiError } from "@/lib/safe-fetch"
